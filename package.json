{
  "name": "twitter-feed-benchmark",
  "version": "1.0.0",
  "description": "Benchmark suite for Push vs. Pull News Feed system design",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --test test/*.test.js",
    "seed": "node benchmark.js seed",
    "benchmark": "node benchmark.js read",
    "docker:up": "docker compose up --build -d",
    "docker:seed": "docker compose exec app node benchmark.js seed",
    "docker:benchmark": "docker compose exec app node benchmark.js read",
    "docker:down": "docker compose down",
    "fanout": "node benchmark.js fanout",
    "docker:fanout": "docker compose exec app node benchmark.js fanout"
  },
  "dependencies": {
    "autocannon": "^7.15.0",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "ioredis": "^5.4.1",
    "pg": "^8.12.0",
    "sqlite3": "^5.1.7"
  }
}
